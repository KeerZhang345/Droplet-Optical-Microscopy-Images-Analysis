This folder contains a minimal, self-contained example demonstrating the droplet_corr_lab analysis pipeline.

The example uses a small subset of raw images and synthetic or regenerated metadata for demonstration purposes.
